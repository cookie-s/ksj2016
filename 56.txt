ifはexp1を評価してからexp2を評価すべきかexp3を評価すべきか選択し、評価すべきでない方はしないのに対して、
if-funをするときでは特に遅延評価などは行われないため、exp1,exp2,exp3をすべて評価してからif-funにその値が渡されて、処理が続行される。
よって、課題のようなコードの場合、fact3は無限ループしてしまって終わらない。

ifとif-funの違いは、
(_f_ #t (display "a") (display "b"))
のような副作用を含むコードを実行してみるとわかりやすい。(_f_をif,if-funそれぞれにして評価してみる。)
